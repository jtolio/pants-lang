parser
 * fix parser and tests to match ast and grammar
 * make semicolons optional (newlines work too)
 * function calls should allow passing things into the function's scope
 * strings
 * comments
 * more unit tests

rest
 * wrap
 * desugar
 * cps_transform
 * rename
 * output c
  * scopes
  * labels
